const text_sub_onchange = () => {
    const subInputValue = document.querySelector('input[name="sub-input"]:checked').value;
    console.log(subInputValue)
    
    switch(subInputValue) {
        case "title": 
    }
    
}